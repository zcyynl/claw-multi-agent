{
  "name": "claw-multi-agent",
  "version": "1.0.0",
  "description": "Multi-agent collaboration for complex tasks — natively built on OpenClaw's sessions_spawn. Each sub-agent runs in an isolated session (no context pollution, lower token cost). Built-in context injection keeps agents aligned without bloating their context. Zero config, zero extra dependencies.",
  "keywords": ["openclaw", "skill", "multi-agent", "swarm", "orchestration", "parallel", "token-efficient", "research", "pipeline"],
  "author": "",
  "license": "MIT",
  "openclaw": {
    "skill": true,
    "entrypoint": "SKILL.md"
  }
}
